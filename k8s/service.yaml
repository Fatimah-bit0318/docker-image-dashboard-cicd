apiVersion: v1
kind: Service

metadata:
  name: docker-dashboard-service

spec:
  type: NodePort

  selector:
    app: docker-dashboard

  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080
